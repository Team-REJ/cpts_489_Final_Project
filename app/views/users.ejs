<!DOCTYPE html>
<html lang="en">
<head><%- include('partials/head') %></head>
<body>
<%- include('partials/navbar') %>

<!-- Page header -->
<div class="page-header">
  <div class="container">
    <div class="d-flex justify-content-between align-items-start">
      <div>
        <h1 class="h3 fw-bold mb-0"><i class="bi bi-people-fill me-2"></i>User Management</h1>
        <p class="text-muted small mb-0">View, search, and manage all registered users.</p>
      </div>
      <span class="badge bg-warning text-dark px-3 py-2"><i class="bi bi-shield-fill me-1"></i>ADMIN</span>
    </div>
  </div>
</div>

<div class="container py-4">
  <%- include('partials/alerts') %>

  <!-- TODO: This page needs Jaydon's admin controller to supply `users` local -->
  <!-- Expected locals: { title, users: [{id, email, first_name, last_name, role, status, created_at}] } -->

  <div class="alert alert-light border text-center py-5">
    <i class="bi bi-hammer fs-1 text-muted"></i>
    <h5 class="fw-bold mt-3">User Management — Under Construction</h5>
    <p class="text-muted mb-3">
      This page is scaffolded and will display a full user table with role and status management
      once the admin controller provides user data.
    </p>
    <a href="/admin" class="btn btn-outline-crimson btn-sm">Back to Admin Dashboard</a>
  </div>

  <!-- Scaffold: User table ready for data -->
  <!--
  <div class="table-section">
    <table class="table table-hover align-middle mb-0">
      <thead>
        <tr>
          <th>User</th>
          <th>Email</th>
          <th>Role</th>
          <th>Status</th>
          <th>Joined</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        // Loop through users array here
      </tbody>
    </table>
  </div>
  -->

</div>

<%- include('partials/footer') %>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
