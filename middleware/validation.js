// Input validation middleware \u2014 stub for Jaydon.
//
// Fill this in with request-body validators as needed. The expected shape is:
//
//   exports.validateCreateListing = (req, res, next) => { ... }
//   exports.validateCreateRequest = (req, res, next) => { ... }
//
// On failure: set req.session.flash = { type: 'error', message } and
// res.redirect('back'), or res.status(400).render(view, { values }) when
// re-rendering the submitted form with its original values.

module.exports = {};
